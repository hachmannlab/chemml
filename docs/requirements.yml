name: chemml
channels:
    - conda-forge
dependencies:
    - python=3.6
    - pip
    - sphinx
    - sphinx_rtd_theme
    - sphinx-automodapi
    - nbsphinx

      # chemml depends
    - rdkit
    - tensorflow

channels:
    - openbabel

dependencies:
    - openbabel

